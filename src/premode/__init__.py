"""Pre-mode Router.

A local-first context compiler plus a privacy-safe Codex CLI wrapper.
The package intentionally exposes from ``src/premode/__init__.py`` and must
not include ``src/premode/init.py``.
"""

__version__ = "0.2.6.9"
