# Superseded verification prompt

This file is intentionally replaced by the current v2.3 implementation and validation report.

Use:

```text
IMPLEMENTATION_REPORT.md
CODEX_ONE_SHOT_PROMPT.md
```

Current validation commands:

```bash
python -m pip install -e ".[dev]"
pytest -q
bash scripts/smoke_test.sh
```
