# Superseded prompt

This file is intentionally replaced by the current v2.3 implementation prompt.

Use:

```text
CODEX_ONE_SHOT_PROMPT.md
```

Current direction:

- default path is deterministic Pre-mode + Codex/GPT
- no local model, Qwen, Ollama, GPU, or model setup is required
- universal project detection and adapters are included
- local/model-assisted behavior belongs only under experimental `premode lab ...` commands
