# v2.5 Patch Plan — Current State

## Folded into this bundle

```text
v2.5 Patch 1 — Repo Map Foundation
v2.5 Patch 2A — Minimal compile integration and entrypoint impact hints
```

## Still planned

```text
v2.5 Patch 2B — Full impact map + verification-order narrowing
v2.5 Patch 3A — Budget report + context receipt + why-included
v2.5 Patch 3B — Manifest grouping + packet modes + token ROI
v2.5 Patch 3C — Packet hashes + exploration/output guidance
```

## Hard rules

- Keep the system agent-agnostic.
- Codex CLI is the first runtime, not the only future runtime.
- Repo-map relevance alone must not promote files to full text.
- Preserve v2.4.5 intake hygiene and proof/control-plane policy behavior.
- Do not add local assist, Ollama, embeddings, vector DBs, tree-sitter, ctags, daemons, patch review, or agent config linting in the remaining v2.5 passes.

## Why Patch 2B still matters

The current `impact_map` can identify deterministic entrypoints for prompts like CLI argument handling.

Patch 2B should broaden that into:

- changed/target file expansion
- direct dependency/dependent lists
- source-to-test matching
- narrow-to-broad verification order
- manifest-informed entrypoint/source scoring across Cargo, Node, Python, Swift, and Go
