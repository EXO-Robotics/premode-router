---
description: Check pCodex status and summarize mode, tuning, fallback, and warnings.
---
# pCodex Status

Run:

```bash
pcodex doctor || true
pcodex status
pcodex status --json
```

Do not modify files. Do not run live Codex tasks.

Summarize configured mode, effective mode, tuning, fallback, MCP, Codex CLI warnings, and Codex config warnings.
